/*
 * FULL_BUILD.c — Full C -> AS -> BIN pipeline orchestration (skeleton).
 *
 * Invoked for -B (BUILD) mode:
 *   1. Preprocess C source
 *   2. Compile C -> AS
 *   3. Preprocess AS output
 *   4. Assemble AS -> BIN
 *
 * TODO: implement once the compiler front-end is functional.
 */
#include "AstraC.h"
