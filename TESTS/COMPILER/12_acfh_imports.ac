#import draw_rect, clear_screen from "graphics.lib"
#import malloc, free from "libc.lib"

U32 main() {
    draw_rect(10, 20, 100, 50, 0x00FF00);
    clear_screen(0x000000);
    U32 ptr = malloc(1024);
    free(ptr);
    return 0;
}
