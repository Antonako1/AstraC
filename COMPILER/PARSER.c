/*
 * COMPILER/PARSER.c — AC language recursive-descent parser (skeleton).
 *
 * Consumes a COMP_TOK_ARRAY and returns a PCNODE tree rooted at CNODE_PROGRAM.
 * TODO: implement.
 */
#include "COMPILER.h"

PCNODE COMP_PARSE(PCOMP_TOK_ARRAY toks, PCOMP_CTX ctx) {
    /* TODO */
    (void)toks; (void)ctx;
    return NULLPTR;
}
