/*
 * COMPILER/VERIFY_AST.c — Semantic verification pass (skeleton).
 *
 * Walks the AST produced by COMP_PARSE() and checks:
 *   - type compatibility, undefined identifiers, arity, return types.
 * TODO: implement.
 */
#include "COMPILER.h"

BOOL COMP_VERIFY(PCNODE root, PCOMP_CTX ctx) {
    /* TODO */
    (void)root; (void)ctx;
    return TRUE;
}
