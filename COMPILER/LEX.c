/*
 * COMPILER/LEX.c — AC language lexer (skeleton).
 *
 * Reads preprocessed AC source (ctx->tmp_src) and produces a COMP_TOK_ARRAY.
 * All identifiers and keywords are normalised to UPPER CASE.
 * TODO: implement.
 */
#include "COMPILER.h"

PCOMP_TOK_ARRAY COMP_LEX(PCOMP_CTX ctx) {
    /* TODO */
    (void)ctx;
    return NULLPTR;
}

VOID DESTROY_COMP_TOK_ARRAY(PCOMP_TOK_ARRAY toks) {
    /* TODO */
    (void)toks;
}
