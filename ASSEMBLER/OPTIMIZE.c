/*
 * ASSEMBLER/OPTIMIZE.c — Peephole optimiser (skeleton).
 *
 * Walks the AST and applies size/speed peephole transformations.
 * Only NODE_INSTRUCTION nodes are inspected.
 * TODO: implement.
 */
#include "ASSEMBLER.h"

BOOLEAN OPTIMIZE(ASM_AST_ARRAY *ast) {
    /* TODO */
    (void)ast;
    return TRUE;
}
